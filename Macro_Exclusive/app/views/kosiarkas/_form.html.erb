<%= form_with(model: kosiarka) do |form| %>
  <% if kosiarka.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kosiarka.errors.count, "error") %> prohibited this kosiarka from being saved:</h2>

      <ul>
        <% kosiarka.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
